# OpenAI Configuration (optional, will fallback to pattern matching if not provided)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Set to false to disable OpenAI and use only pattern matching
USE_OPENAI=true

